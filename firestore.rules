rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone can read car data
    match /cars/{carId} {
      allow read: if true;
      // Only authenticated users can write new data
      allow write: if request.auth != null;
    }
  }
}
